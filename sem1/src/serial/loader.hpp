#include "station.hpp"
#include <filesystem>
#include <vector>
namespace serial::loader {

std::vector<Station>
load_stations(const std::filesystem::path &station_src,
              const std::filesystem::path &measurement_src) {}

} // namespace serial::loader
